The whole UI should be in German, for now we only target German users (students)

Don't use workarounds like ae, oe, ue, but use the proper German "Umlaute" (ä, ö, ü) and "ß" (Eszett) characters in the UI text.

Run `pnpm lint` and `pnpm format:check` after making changes to ensure code quality.
